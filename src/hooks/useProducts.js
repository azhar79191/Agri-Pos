/** useProducts — provides { products, brands, loading, error, fetchProducts, fetchLowStock, addProduct, editProduct, removeProduct, decrementStock } */
export { useProductStore as useProducts } from "../store/productStore";
